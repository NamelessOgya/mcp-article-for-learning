---
trigger: always_on
---

- if you implement something, you must implement test in ./test and  
- if you need to add some python-packages, add it to requirements.txt.
- All code must be executed in the Docker container named "mcp-article".  
- if you implement something, you must rewrite ./docs/functions/{name-of-functio}.md ensure what it does and how to use it.